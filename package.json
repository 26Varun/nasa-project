{
  "name": "nasa-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "server-dev": "npm run watch --prefix server",
    "client-dev": "npm start --prefix client",
    "dev": "npm-run-all --parallel server-dev client-dev",
    "server-prod":"npm run start --prefix server",
    "client-prod":"npm run build --prefix client",
    "prod":"npm run client-prod && npm run server-prod ",
    "prod-cluster":"npm run client-prod && npm run cluster --prefix server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
